# Registry required-check probe

This temporary documentation-only change verifies that `Registry v2 validation / validate-registry` runs on every pull request even when no Registry, script, publication, or workflow path changes.

It is not intended for merge.
